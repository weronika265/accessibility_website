{% include 'default/_form.html.twig' with {
    page_title: 'Odrzuć komentarz',
    page_title_param: {'%id%': comment.id},
    attr: {
        method: 'DELETE',
        action: url('comment_delete', {'id': comment.id}),
    },
    form: form,
    submit_label: 'Odrzuć komentarz'|trans,
    back_to_list_path: 'admin_index',
} %}
